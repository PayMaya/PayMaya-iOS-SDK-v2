clean true

for_platform :ios do
  for_lane :build_appcenter do
    export_method 'enterprise'
    configuration 'Release'
    skip_profile_detection true
    export_options(
      compileBitcode: false,
      signingStyle: 'manual',
      provisioningProfiles: {
        'com.miquido.PaymayaExample' => 'PaymayaExample InHouse'
      }
    )
  end
end
